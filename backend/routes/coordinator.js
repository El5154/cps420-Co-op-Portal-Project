// Coordinator routes
const express = require('express');
const router = express.Router();
const db = require("../config/applicants");

// GET /coordinator - Get coordinator dashboard (coordinator only)

